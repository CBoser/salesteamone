# MindFlow Platform - Project Report

**Generated:** 2025-11-08 11:51:28

## Project Structure

```
salesteamone/
├── backend/          # Express + Prisma API
├── frontend/         # React + Vite UI
├── shared/           # Shared TypeScript types
├── docs/             # Documentation
└── scripts/          # Utility scripts
```

## Technology Stack

### Backend
- **Runtime:** Node.js + TypeScript
- **Framework:** Express.js
- **Database:** PostgreSQL + Prisma ORM
- **Authentication:** JWT + Bcrypt
- **Validation:** Zod

### Frontend
- **Framework:** React 19 + TypeScript
- **Build Tool:** Vite
- **Styling:** Tailwind CSS v4
- **State Management:** React Query (TanStack)
- **Routing:** React Router v7

## Features Implemented

### Customer Management
- ✅ Customer CRUD operations
- ✅ Contact management
- ✅ Pricing tier configuration
- ✅ External system ID mapping
- ✅ Search, filter, and pagination

### Authentication
- ✅ User registration and login
- ✅ JWT token-based authentication
- ✅ Role-based access control (RBAC)
- ✅ Development mode (authentication disabled)

## Database Schema

### Tables
- `users` - User accounts and authentication
- `customers` - Customer records
- `customer_contacts` - Customer contact information
- `customer_pricing_tiers` - Pricing tier history
- `customer_external_ids` - External system mappings
- `jobs` - Future: Job records
- `plans` - Future: Plan records
- `materials` - Future: Material catalog

## API Endpoints

### Authentication (`/api/auth`)
- `POST /register` - Register new user
- `POST /login` - User login
- `GET /me` - Get current user

### Customers (`/api/customers`)
- `GET /` - List customers (with filters)
- `GET /:id` - Get customer by ID
- `POST /` - Create customer
- `PUT /:id` - Update customer
- `DELETE /:id` - Delete customer
- `GET /:id/contacts` - Get customer contacts
- `POST /:id/contacts` - Add contact
- `GET /:id/pricing-tiers` - Get pricing tiers
- `GET /:id/external-ids` - Get external IDs

## Next Steps

1. **Plans Module** - Implement plan CRUD operations
2. **Materials Module** - Build material catalog
3. **Jobs Module** - Create job management system
4. **Takeoffs Module** - Implement material takeoff features
5. **Variance Tracking** - Build variance detection system

## Maintenance Notes

### Database Reset
```bash
python project_manager.py --reset-db
```

### Clean Dependencies
```bash
python project_manager.py --clean
```

### Development Mode
```bash
# Start backend (http://localhost:3001)
cd backend && npm run dev

# Start frontend (http://localhost:5173)
cd frontend && npm run dev
```
